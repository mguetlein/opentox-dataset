require 'rubygems'
require 'rake'

desc "Install required gems"
task :install do
	`sudo gem install sinatra emk-sinatra-url-for builder dm-core dm-more`
end

desc "Run tests"
task :test do
	load 'test.rb'
end

